import { LOCALES } from '../locales';

const resource = Object.freeze({
    [LOCALES["tr-TR"]]: {
        "React Mobile App": "React Mobil Uygulama",
        "Login": "Giriş"
    }
});
export default resource;
